const countLike = document.getElementById("countLike");

function clickLike() {
  countLike.textContent = totalLikes.toString();
  countLike.addEventListener("click", clickLike);
}

function comment() {
  const commentValue = commentbox.value.toString();
  // Add comment logic here
}

// Add event listener to the comment button
commentButton.addEventListener("click", comment);

